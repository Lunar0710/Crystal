{
  "name": "crystal-server",
  "version": "1.0.0",
  "private": true,
  "description": "Relays emotes and cosmetics between Crystal players on the same Minecraft server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
